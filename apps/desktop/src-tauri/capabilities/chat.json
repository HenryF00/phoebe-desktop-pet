{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "chat",
  "description": "The chat window receives only core events and the application-specific commands guarded in Rust.",
  "windows": ["chat"],
  "permissions": ["core:default"]
}
