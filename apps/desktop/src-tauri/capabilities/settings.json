{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "The settings window uses core UI APIs; native settings and autostart remain label-guarded Rust commands.",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
